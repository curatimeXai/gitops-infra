apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: healthmap-worldhealthmap
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:curatimeXai/gitops-infra.git
    targetRevision: HEAD
    path: apps/healthmap-worldhealthmap
  destination:
    server: https://kubernetes.default.svc
    namespace: worldhealthmap
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
