apiVersion: v1
kind: Service
metadata:
  name: heartaware-frontend-service
  namespace: nightingale-heartaware
spec:
  type: ClusterIP
  selector:
    app: heartaware-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
