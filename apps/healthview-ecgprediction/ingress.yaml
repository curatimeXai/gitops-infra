apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ecgprediction-ingress
  namespace: ecgprediction
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ecgprediction.nightingaleheart.com
        - api.ecgprediction.nightingaleheart.com
      secretName: ecgprediction-tls
  rules:
    - host: ecgprediction.nightingaleheart.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ecgprediction-frontend-service
                port:
                  number: 80
    - host: api.ecgprediction.nightingaleheart.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ecgprediction-backend-service
                port:
                  number: 8000
