apiVersion: apps/v1
kind: Deployment
metadata:
  name: worldhealthmap-frontend
  namespace: worldhealthmap
  labels:
    app: worldhealthmap-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: worldhealthmap-frontend
  template:
    metadata:
      labels:
        app: worldhealthmap-frontend
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: worldhealthmap-frontend
          image: ghcr.io/curatimexai/nightingale-worldhealthmap:114ee9faef6533b7bde636a3371e49102e281b8c
          ports:
            - containerPort: 80
