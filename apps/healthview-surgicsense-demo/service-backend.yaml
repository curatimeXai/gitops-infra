apiVersion: v1
kind: Service
metadata:
  name: healthview-surgicsense-demo-backend
  namespace: healthview-surgicsense-demo
  labels:
    app.kubernetes.io/name: healthview-surgicsense-demo-backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: healthview-surgicsense-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: healthview-surgicsense-demo-backend
  ports:
    - name: api
      port: 3001
      targetPort: api
