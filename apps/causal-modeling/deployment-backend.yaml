apiVersion: apps/v1
kind: Deployment
metadata:
 name: causal-modeling-backend
 namespace: causal-modeling
 labels:
    app: causal-modeling-backend
spec:
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: causal-modeling-backend
  template:
    metadata:
      labels:
        app: causal-modeling-backend
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
      - name: causal-modeling-backend
        image: ghcr.io/curatimexai/causal-modeling-backend:4a37a5e17e02e02aebe5064caa8fa85d3af709d0
        ports:
        - containerPort: 8000
        startupProbe:
          tcpSocket:
            port: 8000
          failureThreshold: 30
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 20
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 40
          periodSeconds: 10