apiVersion: v1
kind: Service
metadata:
  name: healthmap-adaptatutor-backend
  namespace: healthmap-adaptatutor
  labels:
    app.kubernetes.io/name: healthmap-adaptatutor-backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: healthmap-adaptatutor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: healthmap-adaptatutor-backend
  ports:
    - name: api
      port: 8000
      targetPort: api
